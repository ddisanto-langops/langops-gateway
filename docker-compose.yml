services:
  gateway:
    build: .
    ports:
      - "3500:3500"
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    env_file:
      - /etc/langops-gateway/secrets.env